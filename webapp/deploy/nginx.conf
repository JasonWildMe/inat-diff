server {
    listen 80;
    server_name invasives.wildme.org;

    # Redirect HTTP to HTTPS (after SSL setup)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }

    # Static files
    location /static {
        alias /opt/invasives/inat-diff/webapp/static;
        expires 7d;
    }

    # Reports (can be large)
    location /report {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_read_timeout 60s;
    }

    # Health check
    location /health {
        proxy_pass http://127.0.0.1:8000;
        proxy_read_timeout 5s;
    }
}

# HTTPS configuration (uncomment after certbot setup)
# server {
#     listen 443 ssl;
#     server_name invasives.wildme.org;
#
#     ssl_certificate /etc/letsencrypt/live/invasives.wildme.org/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/invasives.wildme.org/privkey.pem;
#
#     # ... same location blocks as above ...
# }
